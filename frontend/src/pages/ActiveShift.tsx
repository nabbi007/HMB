import { PagePlaceholder } from "@/components/layout/PagePlaceholder"

export default function ActiveShift() {
  return (
    <PagePlaceholder
      title="Active booking / shift tracker"
      description="Check-in status and the distinct SOS/emergency button land here next."
    />
  )
}
